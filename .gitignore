.DS_Store
gemini-site/
# NOTE: videos/ ships on purpose. The POTW intros must play on a school
# computer with no internet, so they cannot live on YouTube. ~66MB total —
# large for a repo, but a clone has to just work with nothing left to install.
# retired original house art only — new shields/icons/class assets MUST ship
images/camelot.png
images/atlantis.png
images/valhalla.png
images/rivendell.png
images/*-old*.png
docs-in/

# Claude Code local (per-machine) settings
.claude/settings.local.json

# Published by the app at runtime (Data & Safety > Class standings page).
# Committed by the GitHub API rather than by hand, so a local copy from
# testing should never ride along in a commit.
/standings.json
